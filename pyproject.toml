[tool.poetry]
name = "depth-sesing-algo"
version = "0.1.0"
description = "Algorithm for sensing depth"
authors = ["julialva <julio.alvarez@neofield.com.br>"]

[tool.poetry.dependencies]
python = "^3.9"
black = "^22.1.0"
PyOpenGL = "^3.1.6"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
